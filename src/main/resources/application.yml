spring:
  application:
    name: online_store
  profiles:
    active: dev

app:
  api:
    uriPrefix: /api/v1
  sku:
    padding: 8

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html