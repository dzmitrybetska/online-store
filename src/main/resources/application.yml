spring:
  application:
    name: online_store
  datasource:
    url: jdbc:postgresql://localhost:5432/online_store
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: post260125DiMa
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
server:
  port: 8081
